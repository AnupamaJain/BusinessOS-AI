{
  "name": "@whatsapp-smb/scheduler-worker",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@whatsapp-smb/shared-types": "workspace:*",
    "@whatsapp-smb/database": "workspace:*",
    "@whatsapp-smb/mcp-business-tools": "workspace:*"
  },
  "devDependencies": {
    "@whatsapp-smb/config": "workspace:*",
    "@types/node": "^26.1.1",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
